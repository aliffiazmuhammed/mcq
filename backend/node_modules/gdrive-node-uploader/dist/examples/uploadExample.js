"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const progress_1 = __importDefault(require("progress"));
const fileUpload_1 = require("../fileUpload");
const uploadExample = () => __awaiter(void 0, void 0, void 0, function* () {
    const baseDir = path_1.default.resolve(__dirname, '..', '..'); // Navigate one directory up
    const filePath = path_1.default.join(baseDir, 'example_archive.zip');
    try {
        const uploadProgressBar = new progress_1.default('Uploading [:bar] :percent :etas', {
            total: 100,
            width: 40,
            complete: '=',
            incomplete: ' '
        });
        const uploadResult = yield (0, fileUpload_1.uploadFileToGoogleDrive)({
            filePath,
            onProgress: (progress) => {
                uploadProgressBar.update(progress / 100);
            }
        });
        console.log('\nUpload result:', uploadResult);
        if (uploadResult.status) {
            console.log('File uploaded successfully.');
        }
        else {
            console.error('Upload failed.');
        }
    }
    catch (error) {
        console.error('Error during file upload:', error);
    }
});
uploadExample();
//# sourceMappingURL=uploadExample.js.map